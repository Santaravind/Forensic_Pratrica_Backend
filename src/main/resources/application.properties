spring.application.name=forecsic

# Database (PostgreSQL / Neon DB) Configuration
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-noisy-glade-axosfzw2-pooler.c-4.us-east-2.aws.neon.tech/neondb}
spring.datasource.username=${DATABASE_NAME:${DATABASE_USERNAME:neondb_owner}}
spring.datasource.password=${DATABASE_PASSWORD:npg_GW15ECQtgowT}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.org.springframework.security=INFO

# JWT Security
jwt.secret=${JWT_TOKEN:${JWT_SECRET:dGhpc0lzQVZlcnlMb25nU2VjcmV0S2V5Rm9ySldUU2lnbmluZ0F0TGVhc3QyNTZCaXRz}}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# Cloudinary Configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:j9ksfgqo}
cloudinary.api-key=${CLOUDINARY_API_KEY:}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:}

# Resend Email Configuration
resend.api.key=${RESEND_API_KEY:}
resend.from.email=${RESEND_FROM_EMAIL:noreply@forensicpatrika.com}

# OTP Configuration
otp.expiration.minutes=10

# Multipart File Upload (DOC, DOCX, PDF up to 50MB)
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

